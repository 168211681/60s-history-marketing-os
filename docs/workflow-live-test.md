# Archived workflow test

The former provider/render/upload workflow is no longer an active product test. New
requests must return `410 Gone` without contacting a provider or creating a job.

Use the analytics, insights, MCP prompt, and script-review workflow for current
verification. Export to an external editing tool is planned separately.
