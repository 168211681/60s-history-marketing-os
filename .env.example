# Phase 1 runs entirely on labeled sample data. No credentials are required.
# Do not add real secrets to this file. Keep future secrets in .env.local
# or your hosting provider's encrypted environment settings.
# OAuth, database, and AI variables will be defined when those integrations
# are implemented. Adding credentials now does not connect any service.
